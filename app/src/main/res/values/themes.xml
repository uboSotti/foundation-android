<resources>
    <style name="Theme.FoundationAndroid" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- 스플래시 화면 테마. 스플래시 종료 후 postSplashScreenTheme으로 전환된다. -->
    <style name="Theme.FoundationAndroid.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.FoundationAndroid</item>
    </style>
</resources>
